# ui

The browser application: a user's library, the viewer with the boxes over
the page, metrics per page and per book, and the admin panel. Built as
static files into the proxy image; the client is generated from the
backend's OpenAPI document. Not started.
