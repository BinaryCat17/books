# Верхнеуровневые зависимости. Это ВХОД для пересборки constraints.txt —
# ставится на машине именно constraints.txt, см. его шапку.
#
# Закрепление обязательно по двум причинам сразу. Первая: без него разрешение
# зависимостей происходит на арендованной машине во время прогона. Вторая
# осталась от эпохи слоёв — установка по частям без закрепления означала, что
# следующая часть сносит предыдущую: torch удалил поставленные до него
# библиотеки CUDA и скачал свои, а удалённые файлы остались лежать в нижнем
# слое мёртвым грузом на 1.48 ГБ.
vllm==0.27.1
torch==2.13.0
paddlepaddle==3.3.1
onnxruntime-gpu==1.29.0
paddleocr[doc-parser]==3.7.0
pymupdf
huggingface_hub>=1.28
