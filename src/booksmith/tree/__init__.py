"""Instruments over the tree itself: the import graph (`imports`), the shape
of a book directory (`layout`), and one measurement per document
(`figures`). Importable, so the mutation battery can break them and watch a
check go red.

THE CYRILLIC RATCHET WAS THE FOURTH and is deleted -- it could not reach its
own floor, so it counted the permanent and caught nothing. This sentence
named it for a commit after it was gone, which is the same drift the
instruments here exist to measure.
"""

# Imported here so `booksmith.tree.layout` resolves as an ATTRIBUTE: the
# mutation battery swaps the module on this package, and a check that reached
# it any other way would keep the real one and certify nothing.
from booksmith.tree import layout  # noqa: E402,F401
