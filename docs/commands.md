<!-- generated by `books docs`; edit the code, not this file -->

# Commands

```
usage: books [-h]
             {offers,prepare,detect,hybrid,html,crop,fitness,subset,annopage,score,read,apply,text,overlay,synth,bench,ls,down,reap,web,serve,doctor,ledger,replay,docs} ...

Single entry point: books <command>.

    books doctor                 check everything BEFORE the money starts
    books offers                 look at the market, renting nothing
    books prepare book.djvu      djvu -> PDF, spreads cut apart
    books detect book.pdf        LEVEL ONE: contours, local and free
    books hybrid book-dir/       a served hybrid model: boxes and text in one
                                 call, filed as a read run with its own boxes
    books read book.detect/      LEVEL TWO: read the blocks with a model (paid)
    books html book.detect/      build the HTML: text + artefacts as pictures
    books crop book.detect/      what `books read` would send, cut by its own
                                 path; nothing sent, nothing paid
    books apply book-dir/        put the read markup into the book, source out
                                 of its snapshot; repeats are free, what stands
                                 is not placed twice. --status — report only
    books synth                  synthetic bench: pages with exact truth
    books annopage raw/annopage  golden bench: real pages, librarians' truth
    books subset                 distillate: artefacts side by side
    books score truth/ boxes/    contour metrics: boxes, labels, order
    books text truth/ pages/     READING metric: characters and table cells
    books fitness book.pdf …     will the meaning arrive: by ink, no truth
    books overlay book.pdf …     boxes over pages, to look with your own eyes
    books bench all <book>       every applicable metric on one run: one table
    books bench selfcheck <book> every metric's probes: can the numbers fall
    books bench report           every measured number, as METRICS.md
    books serve layout           a detector of the tree's own behind the protocol
    books serve vlm              a vLLM behind describe and health, chat passed through
    books web serve              the backend: users, jobs and HTTP over the service
    books web user <name>        a user of the web, with a store of their own
    books ls | books down 12345 | books reap
    books ledger                 run journal and the estimate from it
    books replay --check out/    is the input snapshot complete
    books docs                   regenerate the documents rendered from the code

This list is `books --help`, and `tests/contract/test_docs.py` checks it
against the parser both ways. Every command with its flags: `docs/commands.md`.

positional arguments:
  {offers,prepare,detect,hybrid,html,crop,fitness,subset,annopage,score,read,apply,text,overlay,synth,bench,ls,down,reap,web,serve,doctor,ledger,replay,docs}
    offers              look at the market, renting nothing
    prepare             unfold djvu into PDF
    detect              level-one contours, locally
    hybrid              boxes and text in one call from a served hybrid model,
                        filed as a read run
    html                build HTML from a books detect directory
    crop                what `books read` would send, by its own path; nothing
                        sent, nothing paid
    fitness             is the output fit to run OCR through
    subset              distillate: artefacts side by side
    annopage            golden bench from the AnnoPage set
    score               contour metrics against the bench truth
    read                LEVEL TWO: read the blocks with a model (paid)
    apply               level two: markup instead of a picture, and undo
    text                reading metric: characters against bench truth
    overlay             boxes over the pages, to look with your own eyes
    synth               synthetic bench with exact truth
    bench               the benches: every metric on one run, side by side
    ls                  what is rented right now
    down                destroy an instance
    reap                destroy everything our runs left behind
    web                 the backend: users, jobs and HTTP over the service
    serve               a model of the tree's own behind the model protocol
    doctor              check the environment before spending money
    ledger              run journal and the estimate from it
    replay              is the input snapshot complete
    docs                regenerate the documents rendered from the code

options:
  -h, --help            show this help message and exit
```

## books offers

```
usage: books offers [-h] [--gpu GPU] [--max-dph MAX_DPH] [--disk DISK]
                    [--machine MACHINE] [--image IMAGE] [--minutes MINUTES]

options:
  -h, --help         show this help message and exit
  --gpu GPU          RTX_4090 / RTX_5090 / A100_PCIE ...
  --max-dph MAX_DPH  ceiling in $/hour
  --disk DISK        instance disk, GB
  --machine MACHINE  pin to a machine_id with a warm cache
  --image IMAGE      override the docker image
  --minutes MINUTES  how many minutes to price a run for
```

## books prepare

```
usage: books prepare [-h] [--out OUT] [--split {auto,yes,no}] file

positional arguments:
  file

options:
  -h, --help            show this help message and exit
  --out OUT             where to put the PDF
  --split {auto,yes,no}
                        whether to cut the spreads
```

## books detect

```
usage: books detect [-h] [--out OUT] [--pages PAGES] [--model MODEL] file

positional arguments:
  file           PDF (unfold djvu with books prepare)

options:
  -h, --help     show this help message and exit
  --out OUT      where to put pages/ and run.json
  --pages PAGES  which pages: 1,4,7-9; all by default
  --model MODEL  an entry of the admin's models.json: its endpoint fills the
                 knob the run reads, its key rides as a secret
```

## books hybrid

```
usage: books hybrid [-h] [--out OUT] [--pages PAGES] [--model MODEL] file

positional arguments:
  file           book directory or PDF

options:
  -h, --help     show this help message and exit
  --out OUT      where to put pages/ and run.json
  --pages PAGES  which pages: 1,4,7-9; all by default
  --model MODEL  an entry of the admin's models.json: its endpoint fills the
                 knob the run reads, its key rides as a secret
```

## books html

```
usage: books html [-h] [--out OUT] dir

positional arguments:
  dir         the directory books detect wrote to

options:
  -h, --help  show this help message and exit
  --out OUT   where to put book.html and assets/
```

## books crop

```
usage: books crop [-h] [--out OUT] [--pages PAGES] [--policy POLICY] dir

positional arguments:
  dir              the directory books detect wrote to

options:
  -h, --help       show this help message and exit
  --out OUT        where to put the crops (default: <dir>.crop)
  --pages PAGES    page numbers to cut, e.g. 1,4,7-9
  --policy POLICY  label dictionary, when the snapshot names none (as `books
                   read` takes it)
```

## books fitness

```
usage: books fitness [-h] --detect DETECT [--truth TRUTH] pdf

positional arguments:
  pdf              the pages to count ink over

options:
  -h, --help       show this help message and exit
  --detect DETECT  model output directory
  --truth TRUTH    truth; without it only what lies outside every box is
                   counted
```

## books subset

```
usage: books subset [-h] [--books BOOKS] [--out OUT]

options:
  -h, --help     show this help message and exit
  --books BOOKS  which bench books, comma-separated
  --out OUT      where to put hard.pdf and truth/
```

## books annopage

```
usage: books annopage [-h] [--split SPLIT] [--limit LIMIT] [--truth-only]
                      [--out OUT]
                      root

positional arguments:
  root           root of the unpacked AnnoPage

options:
  -h, --help     show this help message and exit
  --split SPLIT  test | train
  --limit LIMIT  take only N pages
  --truth-only   rewrite the truth only, leaving the built pdf alone
  --out OUT      where to put annopage.pdf and truth/
```

## books score

```
usage: books score [-h] truth detect

positional arguments:
  truth       truth directory (bench/synth/truth)
  detect      model output directory (…/detect/pages)

options:
  -h, --help  show this help message and exit
```

## books read

```
usage: books read [-h] [--out OUT] [--pages PAGES] [--policy POLICY]
                  [--model MODEL] [--rent] [--budget BUDGET]
                  [--timeout TIMEOUT] [--dry-run] [--key KEY]
                  dir

positional arguments:
  dir                books detect directory

options:
  -h, --help         show this help message and exit
  --out OUT          where to put it; <dir>.read by default
  --pages PAGES      which pages: 1,4,7-9
  --policy POLICY    the detector label dictionary; empty = take it from the
                     detection snapshot, and a mismatch with it is a refusal
                     out loud
  --model MODEL      an entry of the admin's models.json: its endpoint fills
                     the knob the run reads, its key rides as a secret
  --rent             count on a RENTED card instead of VLM_ENDPOINT: take a
                     machine, raise vLLM, fetch the result
  --budget BUDGET    spending ceiling, $; reached — the machine dies
  --timeout TIMEOUT  time ceiling, min; reached — the same
  --dry-run          build the job and check it, renting nothing
  --key KEY          path to the ssh key for vast.ai
```

## books apply

```
usage: books apply [-h] [--anchor ANCHOR] [--file FILE] [--kind KIND]
                   [--source SOURCE] [--undo] [--status] [--from FROM_READ]
                   dir

positional arguments:
  dir               build directory (books html --out)

options:
  -h, --help        show this help message and exit
  --anchor ANCHOR   block anchor, of the form p0042-b17
  --file FILE       file holding the block markup
  --kind KIND       content kind: html | otsl | latex | text
  --source SOURCE   what produced it; goes to the journal and the block
  --undo            bring back what stood before the last swap
  --status          report only: what is replaced, touching nothing
  --from FROM_READ  a `books read` directory: place EVERYTHING read, one block
                    at a time and each with an undo
```

## books text

```
usage: books text [-h] [--norm NORM] truth pages

positional arguments:
  truth        truth directory (bench/<book>/truth)
  pages        directory of what was read (…/detect/pages)

options:
  -h, --help   show this help message and exit
  --norm NORM  the normalisation boundary when comparing; declared as a number
               and carried into the report
```

## books overlay

```
usage: books overlay [-h] [--truth TRUTH] [--detect DETECT] [--out OUT]
                     [--pages PAGES]
                     pdf

positional arguments:
  pdf              the pages to draw over

options:
  -h, --help       show this help message and exit
  --truth TRUTH    truth directory (bench/synth/truth)
  --detect DETECT  model output directory (…/detect/pages)
  --out OUT        where to put the pdf with the boxes
  --pages PAGES    which pages: 1,4,7-9; counted from one, as in detect
```

## books synth

```
usage: books synth [-h] [--book BOOK] [--out OUT] [--cases CASES]

options:
  -h, --help     show this help message and exit
  --book BOOK    which bench book:
                 spravochnik|slovar|matematika|atlas|katalog|zhurnal
  --out OUT      where to put <book>.pdf and truth/
  --cases CASES  which cases, comma-separated; all by default
```

## books bench

```
usage: books bench [-h] {all,selfcheck,report} ...

positional arguments:
  {all,selfcheck,report}
    all                 every applicable metric on one bench and run, as one
                        table and one JSON
    selfcheck           every metric's probes on one bench and run: can the
                        numbers fall (1 if any is uncaught)
    report              every measured number as one generated document, out
                        of the data home's results/; with BOOKSMITH_HOME set
                        that is not the repository's METRICS.md

options:
  -h, --help            show this help message and exit
```

## books bench all

```
usage: books bench all [-h] [--run RUN] [--kind {detect,read}] [--only ONLY]
                       [--json JSON]
                       bench

positional arguments:
  bench                 the book directory, e.g. bench/slovar or
                        processed/ogneupory-vl2. A bench brings truth/ and
                        gets every metric; a book without it gets the truth-
                        free ones

options:
  -h, --help            show this help message and exit
  --run RUN             which run, by its model label. Omit it when the book
                        has exactly one of this kind; with several, the
                        command refuses and lists them
  --kind {detect,read}  which level to measure: detect, the contours
                        (default), or read, the level-two reading
  --only ONLY           comma-separated metric names, instead of every
                        applicable one
  --json JSON           where to write the records (default:
                        results/<bench>-<run>.json, and
                        <bench>-<kind>-<run>.json for a level that is not
                        detect: a label is the model's own name and two levels
                        can share one)
```

## books bench selfcheck

```
usage: books bench selfcheck [-h] [--run RUN] [--kind {detect,read}]
                             [--only ONLY]
                             bench

positional arguments:
  bench                 the book directory, as `bench all` takes it

options:
  -h, --help            show this help message and exit
  --run RUN             which run, by its model label; omit it when the book
                        has exactly one of this kind
  --kind {detect,read}  which level to probe
  --only ONLY           comma-separated metric names, instead of every
                        applicable one
```

## books bench report

```
usage: books bench report [-h] [--out OUT]

options:
  -h, --help  show this help message and exit
  --out OUT   where to write it (default: METRICS.md in the data home)
```

## books ls

```
usage: books ls [-h]

options:
  -h, --help  show this help message and exit
```

## books down

```
usage: books down [-h] id

positional arguments:
  id

options:
  -h, --help  show this help message and exit
```

## books reap

```
usage: books reap [-h]

options:
  -h, --help  show this help message and exit
```

## books web

```
usage: books web [-h] {serve,user} ...

positional arguments:
  {serve,user}
    serve       run the backend until stopped
    user        a user of the web, with a store of their own; the password
                from BOOKSMITH_PASSWORD or a prompt

options:
  -h, --help    show this help message and exit
```

## books web serve

```
usage: books web serve [-h] [--host HOST] [--port PORT]

options:
  -h, --help   show this help message and exit
  --host HOST
  --port PORT
```

## books web user

```
usage: books web user [-h] [--role {admin,user}] name

positional arguments:
  name

options:
  -h, --help           show this help message and exit
  --role {admin,user}
```

## books serve

```
usage: books serve [-h] {layout,vlm} ...

positional arguments:
  {layout,vlm}
    layout      the detector LAYOUT_ADAPTER names, behind describe, health and
                layout
    vlm         a vLLM behind describe and health, the chat route passed
                through

options:
  -h, --help    show this help message and exit
```

## books serve layout

```
usage: books serve layout [-h] [--host HOST] [--port PORT]

options:
  -h, --help   show this help message and exit
  --host HOST
  --port PORT
```

## books serve vlm

```
usage: books serve vlm [-h] [--host HOST] [--port PORT] [--upstream UPSTREAM]
                       [--log-dir LOG_DIR]

options:
  -h, --help           show this help message and exit
  --host HOST
  --port PORT
  --upstream UPSTREAM  a vLLM already up, its root without /v1; empty raises
                       one here over VL_MODEL_DIR
  --log-dir LOG_DIR    where vllm.log goes
```

## books doctor

```
usage: books doctor [-h]

options:
  -h, --help  show this help message and exit
```

## books ledger

```
usage: books ledger [-h]

options:
  -h, --help  show this help message and exit
```

## books replay

```
usage: books replay [-h] [--check] outdir [outdir ...]

positional arguments:
  outdir      parse directory

options:
  -h, --help  show this help message and exit
  --check     print what is missing and return 1 if there is any
```

## books docs

```
usage: books docs [-h]

options:
  -h, --help  show this help message and exit
```
