# 2026-09-06: baseline before the clean slate

Measured on commit c69684f (branch chistyy-list) before any change.

| instrument | value |
|---|---|
| fast suite | checks 304: passed 303, failed 0, skipped 1; 37.4 s |
| mutation anchors (tools/anchors.py) | 73 land, 0 do not land, 0 not literal |
| acceptance (tools/acceptance.py) | 7 reports, all same |
| Cyrillic ratchet | 1105 under the ratchet; 468 book prose, 10820 rename record, 17709 bench data exempt |
| contour battery on slovar | probes 33, measured 26, no data 7, uncaught 0 |
| reading battery on slovar (truth vs truth) | probes 29, measured 19, no data 10, uncaught 0 |
| fitness battery on slovar | probes 21, measured 20, no data 1, uncaught 1: "the dark column blown up and squeezed" |
| prose ratio, src (tokenize: comment + docstring lines over all non-blank) | 40.5 %; models 51.4, run 51.4, remote 48.0, paddleocr_vl 46.8, read 42.7, doc 42.5, root 35.7, books 24.3 |
| prose ratio, tests | 35.0 % |
| slovar rebuild with seed 1, aging old | 13 of 13 truth files byte-identical, PDF sha256 identical (8f607163…) |

Claims settled by these measurements:

* "33 probes" for the contour battery is TRUE on this tree. The audit's static
  count of 28 was wrong; the battery counts probes in three loops.
* The synthetic bench is deterministic end to end, fonts and aging included.
  A split of synth.py can therefore be proved by rebuilding and comparing.
* bench/slovar/manifest.json still carries Russian in `knobs.*.what` and
  `generator.commit` (the Russian words for "dirty tree"): a record of the build that made it,
  exempt from the ratchet as bench data, rewritten when the bench is rebuilt.
| slow battery (tests/run.py --slow --selfcheck) | checks 304 passed 304, skipped 0 with --slow; mutations 286: caught 286, uncaught 0; 299 of 304 checks run under a mutation, 5 under none |
| metric result dicts | JSON-serialisable after rounding: score-annopage 64 KB, score-hard 18 KB, text-slovar 84 KB, fitness-slovar 754 bytes |
| fitness on annopage (600 pages) | objects 1230: intact 1018 (83 %), almost intact 61, bitten 24, torn 127; WARNING 2 truth objects without ink |

* "1232 vs 1230" is not a contradiction: score counts every truth artefact
  (1232), fitness counts those with ink at 144 dpi and warns about the two
  without. Both numbers are right and the warning names the difference. The
  table must carry the denominator as a column, not reconcile it.
| slow battery duration | 114 s (measured by the feasibility reviewer on the same tree; the first run here lost its timing to a redirect) |
| fast suite, second machine state | 32.0 s, same counts (37.4 s on the first run with agents loading the CPU) |

## Step 0 locks, proved 2026-09-07

`COVER_MATCH` moved from 0.75 to 0.5 for one run of the acceptance:

| lock | verdict |
|---|---|
| record score-annopage | CHANGED, 126 paths |
| record score-hard | CHANGED, 64 paths |
| record text-slovar | CHANGED, 1 path |
| record fitness-slovar | same |
| report score-annopage | CHANGED, 173 lines |
| report score-hard | CHANGED, 107 lines |
| report text-slovar | same |
| report score-selfcheck | CHANGED, 29 lines |
| the other five reports | same |

The one path in the text-slovar RECORD moved while the text-slovar REPORT
did not: the reading metric's geometry pairing borrows the contour metric's
gate, and no printed line of the reading report carries it. That is the
blind spot the records were added for, found on their first run.

`tools/anchors.py` with one `attrs()` name misspelt: "NO ATTR
selfcheck.py:3310: dhtml has no repeats_onn", exit 1. Restored.

The contour battery's report was not stable across processes: the probe
"label X replaced by chart" chose X by `max` over a SET on a tie (`table`
and `image` tie on slovar), so the line read `table` in one run and `image`
in the next. Found by the new text lock within an hour of its creation, when
`tools/acceptance.py` disagreed with itself between two runs. Fixed by
sorting before `max`, proved by three runs under `PYTHONHASHSEED` 1, 2, 3.
The quoted "33 probes" was never wrong; the probe's NAME was.
